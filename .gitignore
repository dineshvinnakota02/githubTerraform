# Local .terraform directories
**/.terraform*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data.
# These should not be part of version control as they are data points
# which are potentially sensitive and subject to change per environment.
# Some teams choose to include a dummy terraform.tfvars.sample file in repo.
# terraform.tfvars
# *.auto.tfvars

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Optional: ignore plan files saved before destroying Terraform configuration
# Uncomment the line below if you want to ignore planout files.
# planout

# Optional: ignore graph output files generated by `terraform graph`
# *.dot